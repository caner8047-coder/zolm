<div class="space-y-4 lg:space-y-6"><div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-3"><div><h1 class="text-xl lg:text-2xl font-semibold text-slate-900">Bordro Hazırlık</h1><p class="mt-1 text-sm text-slate-500">Kapanmış puantajı kaynak iziyle dondurun; bu ekran ücret veya vergi hesabı yapmaz.</p></div>@if(auth()->user()?->hasHrPermission('hr.payroll.manage_rules'))<a href="{{ route('hr.settings.payroll-rules') }}" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] border border-slate-200 bg-white text-center text-sm">Kural sürümleri</a>@endif</div>@if(session('success'))<div class="rounded-[8px] border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif
@if(auth()->user()?->hasHrPermission('hr.payroll.calculate'))<form wire:submit="prepare" class="rounded-[10px] border border-slate-200 bg-white p-4 lg:p-6 shadow-sm"><div class="grid grid-cols-1 sm:grid-cols-2 gap-3"><select wire:model.defer="timesheetPeriodId" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2"><option value="">Kapanmış puantaj dönemi seçin</option>@foreach($closedTimesheets as $item)<option value="{{ $item->id }}">{{ $item->name }} · {{ $item->starts_on->format('d.m.Y') }}–{{ $item->ends_on->format('d.m.Y') }}</option>@endforeach</select><button class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] bg-slate-900 text-sm text-white">Hazırlık paketi oluştur</button></div>@error('timesheetPeriodId')<p class="mt-2 text-xs text-red-600">{{ $message }}</p>@enderror</form>@endif
<div class="grid grid-cols-1 xl:grid-cols-4 gap-3 lg:gap-4"><aside class="rounded-[10px] border border-slate-200 bg-white shadow-sm overflow-hidden"><div class="border-b border-slate-200 bg-slate-50/60 p-4 text-sm font-semibold text-slate-900">Paketler</div><div class="divide-y divide-slate-100">@forelse($periods as $item)<button wire:click="select({{ $item->id }})" class="w-full p-4 text-left {{ $selectedPeriodId===$item->id?'bg-slate-50':'' }}"><span class="block text-sm font-medium text-slate-900">{{ $item->name }}</span><span class="mt-1 block text-xs text-slate-500">{{ $item->records_count }} çalışan · {{ $item->status }}</span></button>@empty<div class="p-6 text-sm text-slate-500">Paket yok.</div>@endforelse</div></aside><section class="xl:col-span-3 rounded-[10px] border border-slate-200 bg-white shadow-sm overflow-hidden">@if($selected)<div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 border-b border-slate-200 bg-slate-50/60 p-4"><div><h2 class="text-sm font-semibold text-slate-900">{{ $selected->name }}</h2><p class="mt-1 text-xs text-slate-500">Kaynak: {{ $selected->timesheetPeriod?->name }} · {{ substr($selected->source_hash,0,12) }}…</p></div>@if($selected->status==='prepared' && auth()->user()?->hasHrPermission('hr.payroll.approve'))<button wire:click="approve" wire:confirm="Paket onaylanıp dondurulsun mu?" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] bg-slate-900 text-sm text-white">Onayla ve dondur</button>@else<span class="px-2 py-0.5 text-xs font-mono rounded bg-emerald-50 text-emerald-700">{{ $selected->status }}</span>@endif</div><div class="hidden md:block overflow-x-auto rounded-lg"><table class="w-full table-fixed"><thead class="bg-slate-50/60 text-left text-xs uppercase text-slate-500"><tr><th class="px-4 py-3">Çalışan</th><th class="px-4 py-3">Çalışma</th><th class="px-4 py-3">İzin</th><th class="px-4 py-3">Fazla</th><th class="px-4 py-3">Onaylı fazla</th><th class="px-4 py-3">Eksik</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($selected->records as $record)<tr><td class="overflow-hidden text-ellipsis px-4 py-3 text-sm font-medium text-slate-900">{{ $record->employee?->full_name }}</td>@foreach(['worked_minutes','leave_minutes','overtime_minutes','approved_overtime_minutes','missing_minutes'] as $field)<td class="px-4 py-3 text-sm text-slate-600">{{ intdiv($record->$field,60) }}s {{ $record->$field%60 }}dk</td>@endforeach</tr>@endforeach</tbody></table></div><div class="md:hidden divide-y divide-slate-100">@foreach($selected->records as $record)<article class="p-4"><p class="text-sm font-medium text-slate-900">{{ $record->employee?->full_name }}</p><p class="mt-1 text-xs text-slate-500">Çalışma {{ $record->worked_minutes }}dk · Onaylı fazla {{ $record->approved_overtime_minutes }}dk · Eksik {{ $record->missing_minutes }}dk</p></article>@endforeach</div>@else<div class="p-10 text-center text-sm text-slate-500">Bir bordro hazırlık paketi seçin.</div>@endif</section></div></div>
