<div class="space-y-4 lg:space-y-6">
    <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-3"><div><h1 class="text-xl lg:text-2xl font-semibold text-slate-900">{{ $selfService ? 'Fazla Mesailerim' : 'Fazla Mesai Talepleri' }}</h1><p class="mt-1 text-sm text-slate-500">Talep, yönetici ve İK onay adımlarını tek akışta izleyin.</p></div><div class="flex flex-col sm:flex-row gap-2">@if(!$selfService)<a href="{{ route('hr.timesheets') }}" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] border border-slate-200 bg-white text-center text-sm text-slate-700">Puantaj</a>@if(auth()->user()?->hasHrPermission('hr.timesheet.close'))<a href="{{ route('hr.settings.overtime-types') }}" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] bg-slate-900 text-center text-sm text-white">Tür ayarları</a>@endif @endif</div></div>
    @if(session('success'))<div class="rounded-[8px] border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif
    <section x-data="{open:{{ $selfService ? 'true' : 'false' }}}" class="rounded-[10px] border border-slate-200 bg-white shadow-sm overflow-hidden"><button type="button" @click="open=!open" class="flex w-full items-center justify-between p-4 text-left"><span><span class="block text-sm font-semibold text-slate-900">Yeni fazla mesai talebi</span><span class="mt-1 block text-xs text-slate-500">Üretim emri veya proje referansı ekleyebilirsiniz.</span></span><span x-text="open?'−':'+'"></span></button><form x-show="open" x-cloak wire:submit="create" class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4 gap-3 lg:gap-4 border-t border-slate-200 bg-slate-50/60 p-4">@if(!$selfService)<select wire:model.defer="employeeId" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2"><option value="">Çalışan seçin</option>@foreach($employees as $employee)<option value="{{ $employee->id }}">{{ $employee->full_name }}</option>@endforeach</select>@endif<select wire:model.defer="typeId" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2"><option value="">Fazla mesai türü</option>@foreach($types as $type)<option value="{{ $type->id }}">{{ $type->name }} · x{{ $type->multiplier }}</option>@endforeach</select><input type="date" wire:model.defer="workDate" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2"><div class="grid grid-cols-2 gap-2"><input type="time" wire:model.defer="startsAt" class="min-w-0 text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2"><input type="time" wire:model.defer="endsAt" class="min-w-0 text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2"></div><input wire:model.defer="reason" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2" placeholder="Gerekçe"><input wire:model.defer="projectReference" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2" placeholder="Proje referansı"><input wire:model.defer="productionOrderReference" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2" placeholder="Üretim emri"><button class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] bg-slate-900 text-sm text-white">Talep oluştur</button>@if($errors->any())<p class="sm:col-span-2 xl:col-span-4 text-xs text-red-600">{{ $errors->first() }}</p>@endif</form></section>
    <section class="rounded-[10px] border border-slate-200 bg-white shadow-sm overflow-hidden"><div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-3 border-b border-slate-200 bg-slate-50/60 p-4">@if(!$selfService)<input wire:model.live.debounce.300ms="search" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2 xl:col-span-2" placeholder="Çalışan veya sicil ara">@endif<select wire:model.live="statusFilter" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2"><option value="">Tüm durumlar</option><option value="pending_manager">Yönetici bekliyor</option><option value="pending_hr">İK bekliyor</option><option value="approved">Onaylandı</option><option value="rejected">Reddedildi</option></select></div><div class="hidden md:block overflow-x-auto rounded-lg"><table class="w-full table-fixed"><thead class="bg-slate-50/60 text-left text-xs uppercase text-slate-500"><tr><th class="px-4 py-3">Çalışan</th><th class="px-4 py-3">Tarih</th><th class="px-4 py-3">Tür</th><th class="px-4 py-3">Saat / süre</th><th class="px-4 py-3">Referans</th><th class="px-4 py-3">Durum</th><th class="px-4 py-3 text-right">İşlem</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($requests as $request)<tr><td class="overflow-hidden text-ellipsis px-4 py-3 text-sm font-medium text-slate-900">{{ $request->employee?->full_name }}</td><td class="px-4 py-3 text-sm text-slate-600">{{ $request->work_date->format('d.m.Y') }}</td><td class="overflow-hidden text-ellipsis px-4 py-3 text-sm text-slate-600">{{ $request->overtimeType?->name }}</td><td class="px-4 py-3 text-sm text-slate-600">{{ substr($request->starts_at,0,5) }}–{{ substr($request->ends_at,0,5) }} · {{ $request->requested_minutes }}dk</td><td class="overflow-hidden text-ellipsis px-4 py-3 text-xs text-slate-500">{{ $request->production_order_reference ?: ($request->project_reference ?: '—') }}</td><td class="px-4 py-3"><span class="px-2 py-0.5 text-xs font-mono rounded bg-slate-100 text-slate-700">{{ $request->status->label() }}</span></td><td class="px-4 py-3 text-right">@if(!$selfService && in_array($request->status->value,['pending_manager','pending_hr'],true) && auth()->user()?->hasHrPermission('hr.timesheet.confirm'))<button wire:click="startDecision({{ $request->id }})" class="px-3 py-2 text-xs text-slate-700">Karar ver</button>@endif</td></tr>@empty<tr><td colspan="7" class="px-4 py-12 text-center text-sm text-slate-500">Fazla mesai talebi bulunamadı.</td></tr>@endforelse</tbody></table></div><div class="md:hidden divide-y divide-slate-100">@forelse($requests as $request)<article class="p-4"><div class="flex items-start justify-between gap-3"><div class="min-w-0"><p class="truncate text-sm font-medium text-slate-900">{{ $request->employee?->full_name }}</p><p class="mt-1 text-xs text-slate-500">{{ $request->work_date->format('d.m.Y') }} · {{ substr($request->starts_at,0,5) }}–{{ substr($request->ends_at,0,5) }} · {{ $request->overtimeType?->name }}</p></div><span class="px-2 py-0.5 text-xs font-mono rounded bg-slate-100">{{ $request->status->label() }}</span></div>@if(!$selfService && in_array($request->status->value,['pending_manager','pending_hr'],true))<button wire:click="startDecision({{ $request->id }})" class="mt-3 w-full px-4 py-3 rounded-[6px] border border-slate-200 text-sm">Karar ver</button>@endif</article>@empty<div class="p-8 text-center text-sm text-slate-500">Talep bulunamadı.</div>@endforelse</div><div class="border-t border-slate-200 p-4">{{ $requests->links() }}</div></section>
    @if($decidingId)<div class="fixed inset-0 z-50 flex items-end sm:items-center justify-center bg-slate-900/40 p-4"><div class="w-full max-w-md rounded-[10px] border border-slate-200 bg-white p-4 lg:p-6 shadow-md"><h2 class="text-lg font-semibold text-slate-900">Fazla mesai kararı</h2><input type="number" min="1" wire:model.defer="approvedMinutes" class="mt-4 w-full text-base sm:text-sm rounded-[6px] border border-slate-200 px-3 py-3 sm:py-2" placeholder="Onaylanan dakika"><textarea wire:model.defer="decisionNote" rows="3" class="mt-3 w-full text-base sm:text-sm rounded-[6px] border border-slate-200 px-3 py-3 sm:py-2" placeholder="Karar notu"></textarea>@if($errors->any())<p class="mt-2 text-xs text-red-600">{{ $errors->first() }}</p>@endif<div class="mt-4 grid grid-cols-1 sm:grid-cols-3 gap-2"><button wire:click="$set('decidingId',null)" class="w-full px-4 py-3 sm:py-2 rounded-[6px] border border-slate-200 text-sm">Vazgeç</button><button wire:click="reject" class="w-full px-4 py-3 sm:py-2 rounded-[6px] border border-red-200 text-sm text-red-700">Reddet</button><button wire:click="approve" class="w-full px-4 py-3 sm:py-2 rounded-[6px] bg-slate-900 text-sm text-white">Onayla</button></div></div></div>@endif
</div>
