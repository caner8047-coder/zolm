<div class="space-y-4 lg:space-y-6"><div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-3"><div><h1 class="text-xl lg:text-2xl font-semibold text-slate-900">İşe Giriş & Çıkış (Lifecycle)</h1><p class="mt-1 text-sm text-slate-500">Tekliften çalışana dönüşüm, 7/30/60/90 planı, kontrollü çıkış ve tazminat hesaplayıcı.</p></div><div><a href="{{ route('hr.severance-calculator') }}" class="inline-flex items-center gap-2 rounded-[6px] border border-slate-200 bg-white px-4 py-2 text-sm font-medium text-slate-700 shadow-sm hover:bg-slate-50">🧮 Kıdem ve İhbar Tazminatı Hesaplayıcı</a></div></div>@if(session('success'))<div class="rounded-[8px] border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif
<section class="grid grid-cols-1 xl:grid-cols-2 gap-3 lg:gap-4"><div class="rounded-[10px] border border-slate-200 bg-white shadow-sm overflow-hidden"><div class="border-b p-4"><h2 class="text-sm font-semibold">İşe alıma hazır teklifler</h2></div><div class="divide-y">@forelse($offers as $offer)<div class="p-4"><p class="text-sm font-medium">{{ $offer->application?->candidate?->full_name }}</p><p class="text-xs text-slate-500">Başlangıç {{ $offer->proposed_start_date->format('d.m.Y') }}</p><div class="mt-3 flex flex-col sm:flex-row gap-2"><input wire:model.defer="hireNationalIds.{{ $offer->id }}" type="password" inputmode="numeric" class="min-w-0 flex-1 text-base sm:text-sm rounded-[6px] border px-3 py-3" placeholder="Kimlik numarası"><button wire:click="hire({{ $offer->id }})" wire:confirm="Aday çalışan kaydına dönüştürülsün mü?" class="w-full sm:w-auto px-4 py-3 rounded-[6px] bg-slate-900 text-sm text-white">İşe al</button></div></div>@empty<div class="p-6 text-sm text-slate-500">Onaylı teklif yok.</div>@endforelse</div></div><form wire:submit="startOffboarding" class="rounded-[10px] border border-slate-200 bg-white p-4 shadow-sm"><h2 class="text-sm font-semibold">Offboarding başlat</h2><div class="mt-3 space-y-3"><select wire:model.defer="offboardingEmployeeId" class="w-full text-base sm:text-sm rounded-[6px] border px-3 py-3"><option value="">Çalışan</option>@foreach($employees as $employee)<option value="{{ $employee->id }}">{{ $employee->full_name }}</option>@endforeach</select><input wire:model.defer="lastWorkingOn" type="date" class="w-full text-base sm:text-sm rounded-[6px] border px-3 py-3"><textarea wire:model.defer="terminationReason" class="w-full text-base sm:text-sm rounded-[6px] border px-3 py-3" placeholder="Çıkış gerekçesi"></textarea><button class="w-full px-4 py-3 rounded-[6px] bg-slate-900 text-white">Kontrol listesini başlat</button></div></form></section>
@foreach([['title'=>'Onboarding 7/30/60/90','rows'=>$onboardings,'kind'=>'on'],['title'=>'Offboarding kontrolleri','rows'=>$offboardings,'kind'=>'off']] as $section)<section class="rounded-[10px] border border-slate-200 bg-white shadow-sm overflow-hidden"><div class="border-b p-4"><h2 class="text-sm font-semibold">{{ $section['title'] }}</h2></div><div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-3 p-4">@forelse($section['rows'] as $checklist)<article class="rounded-[8px] border bg-slate-50/70 p-4"><div class="flex justify-between gap-2"><div><p class="text-sm font-medium">{{ $checklist->employee?->full_name }}</p><p class="text-xs text-slate-500">{{ $checklist->status==='completed'?'Tamamlandı':'Aktif' }}</p></div><span class="text-xs font-mono">{{ $checklist->tasks->where('status','completed')->count() }}/{{ $checklist->tasks->count() }}</span></div><div class="mt-3 space-y-2">@foreach($checklist->tasks as $task)<div class="rounded-[6px] border bg-white p-3"><p class="text-xs {{ $task->status==='completed'?'text-emerald-700':'text-slate-700' }}">{{ $task->title }}</p>@if($task->status==='pending')<div class="mt-2 flex gap-1"><input wire:model.defer="taskEvidence.{{ $section['kind'] }}_{{ $task->id }}" class="min-w-0 flex-1 rounded-[6px] border px-2 py-2 text-sm" placeholder="Kanıt / not"><button wire:click="{{ $section['kind']==='on'?'completeOnboardingTask':'completeOffboardingTask' }}({{ $task->id }})" class="px-2 py-2 text-xs text-indigo-700">Tamamla</button></div>@endif</div>@endforeach</div>@if($section['kind']==='off'&&$checklist->status==='active')<button wire:click="closeOffboarding({{ $checklist->id }})" wire:confirm="Çalışan çıkışı kapatılsın mı?" class="mt-3 w-full px-4 py-3 rounded-[6px] border text-sm text-red-700">Çıkışı kapat</button>@endif</article>@empty<p class="text-sm text-slate-500">Kayıt yok.</p>@endforelse</div></section>@endforeach</div>
