<div class="space-y-4 lg:space-y-6">
<div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-3"><div><h1 class="text-xl lg:text-2xl font-semibold text-slate-900">{{ $selfService?'Eğitimlerim':'Eğitim & Gelişim' }}</h1><p class="mt-1 text-sm text-slate-500">Katalog, oturum, katılım, sınav ve süreli sertifikaları tek kayıt zincirinde yönetin.</p></div>@if(!$selfService)<a href="{{ route('hr.dashboard') }}" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] border border-slate-200 bg-white text-center text-sm">İK merkezi</a>@endif</div>
@if(session('success'))<div class="rounded-[8px] border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif
@if(!$selfService && auth()->user()?->hasHrPermission('hr.training.manage'))
<section x-data="{course:false,session:false}" class="rounded-[10px] border border-slate-200 bg-white shadow-sm overflow-hidden"><div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 p-4"><div><h2 class="text-sm font-semibold text-slate-900">Katalog ve oturum kontrolü</h2><p class="mt-1 text-xs text-slate-500">Zorunluluk, geçme puanı ve sertifika geçerliliği katalogda tanımlanır.</p></div><div class="flex flex-col sm:flex-row gap-2"><button @click="course=!course" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] border text-sm">+ Eğitim</button><button @click="session=!session" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] bg-slate-900 text-sm text-white">+ Oturum</button></div></div>
<form x-show="course" wire:submit="createCourse" class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-3 border-t bg-slate-50/60 p-4"><input wire:model.defer="courseCode" class="text-base sm:text-sm rounded-[6px] border px-3 py-3" placeholder="Eğitim kodu"><input wire:model.defer="courseTitle" class="text-base sm:text-sm rounded-[6px] border px-3 py-3" placeholder="Eğitim adı"><input wire:model.defer="courseCategory" class="text-base sm:text-sm rounded-[6px] border px-3 py-3" placeholder="Kategori"><input wire:model.defer="durationMinutes" type="number" min="0" class="text-base sm:text-sm rounded-[6px] border px-3 py-3" placeholder="Süre (dk)"><input wire:model.defer="passingScore" type="number" min="0" max="100" class="text-base sm:text-sm rounded-[6px] border px-3 py-3" placeholder="Geçme puanı"><input wire:model.defer="validityMonths" type="number" min="1" class="text-base sm:text-sm rounded-[6px] border px-3 py-3" placeholder="Sertifika geçerliliği (ay)"><label class="flex items-center gap-2 text-sm"><input type="checkbox" wire:model.defer="isMandatory"> Zorunlu eğitim</label><button class="w-full sm:w-auto px-4 py-3 rounded-[6px] bg-slate-900 text-white">Kataloğa ekle</button></form>
<form x-show="session" wire:submit="scheduleSession" class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-3 border-t bg-slate-50/60 p-4"><select wire:model.defer="courseId" class="text-base sm:text-sm rounded-[6px] border px-3 py-3"><option value="">Eğitim</option>@foreach($courses as $course)<option value="{{ $course->id }}">{{ $course->title }}</option>@endforeach</select><select wire:model.defer="deliveryType" class="text-base sm:text-sm rounded-[6px] border px-3 py-3"><option value="classroom">Sınıf</option><option value="online">Çevrim içi</option><option value="hybrid">Hibrit</option></select><input wire:model.defer="instructor" class="text-base sm:text-sm rounded-[6px] border px-3 py-3" placeholder="Eğitmen"><input wire:model.defer="location" class="text-base sm:text-sm rounded-[6px] border px-3 py-3" placeholder="Konum / bağlantı"><input wire:model.defer="startsAt" type="datetime-local" class="text-base sm:text-sm rounded-[6px] border px-3 py-3"><input wire:model.defer="endsAt" type="datetime-local" class="text-base sm:text-sm rounded-[6px] border px-3 py-3"><input wire:model.defer="capacity" type="number" min="1" class="text-base sm:text-sm rounded-[6px] border px-3 py-3" placeholder="Kontenjan (boş=sınırsız)"><button class="w-full sm:w-auto px-4 py-3 rounded-[6px] bg-slate-900 text-white">Oturumu planla</button></form></section>
@endif
@if(!$selfService && auth()->user()?->hasHrPermission('hr.training.enroll'))
<section class="rounded-[10px] border border-slate-200 bg-white shadow-sm p-4"><div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3"><div><h2 class="text-sm font-semibold">Eğitime çalışan ata</h2><p class="mt-1 text-xs text-slate-500">Kontenjan sunucu tarafında kilitlenerek kontrol edilir.</p></div><div class="grid grid-cols-1 sm:grid-cols-3 gap-2 w-full sm:w-auto"><select wire:model.defer="sessionId" class="text-base sm:text-sm rounded-[6px] border px-3 py-3"><option value="">Oturum</option>@foreach($sessions as $session)<option value="{{ $session->id }}">{{ $session->course?->title }} · {{ $session->starts_at->format('d.m.Y') }} ({{ $session->enrollments_count }}/{{ $session->capacity??'∞' }})</option>@endforeach</select><select wire:model.defer="employeeId" class="text-base sm:text-sm rounded-[6px] border px-3 py-3"><option value="">Çalışan</option>@foreach($employees as $employee)<option value="{{ $employee->id }}">{{ $employee->full_name }}</option>@endforeach</select><button wire:click="enroll" class="w-full sm:w-auto px-4 py-3 rounded-[6px] bg-slate-900 text-white">Ata</button></div></div></section>
@endif
<section class="rounded-[10px] border border-slate-200 bg-white shadow-sm overflow-hidden"><div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 border-b bg-slate-50/60 p-4"><div><h2 class="text-sm font-semibold">Katılım defteri</h2><p class="text-xs text-slate-500">İlerleme ve sınav sonucu değiştirilemez tamamlanma kaydına dönüşür.</p></div><div class="flex gap-2"><select wire:model.live="statusFilter" class="text-base sm:text-sm rounded-[6px] border bg-white px-3 py-3 sm:py-2"><option value="">Tüm durumlar</option><option value="enrolled">Kayıtlı</option><option value="in_progress">Devam ediyor</option><option value="completed">Tamamlandı</option><option value="failed">Başarısız</option></select><div x-data="{open:false}" class="relative"><button @click="open=!open" class="px-4 py-3 sm:py-2 rounded-[6px] border bg-white text-sm">Kolonlar · {{ count($visibleColumns) }}</button><div x-show="open" x-cloak @click.outside="open=false" class="absolute right-0 z-20 mt-2 w-48 rounded-[8px] border bg-white p-2 shadow-md">@foreach($columnLabels as $key=>$label)<label class="flex gap-2 px-2 py-2 text-sm"><input type="checkbox" wire:click="toggleColumn('{{ $key }}')" @checked(in_array($key,$visibleColumns,true)) @disabled(in_array($key,['employee','actions'],true))>{{ $label }}</label>@endforeach</div></div></div></div>
<div class="hidden md:block overflow-x-auto rounded-lg" x-data="columnResize()"><table class="w-full table-fixed"><thead class="bg-slate-50/60 text-left text-xs uppercase text-slate-500"><tr>@foreach($visibleColumns as $column)<th class="px-4 py-3">{{ $columnLabels[$column] }}</th>@endforeach</tr></thead><tbody class="divide-y">@forelse($enrollments as $row)<tr>@foreach($visibleColumns as $column)@switch($column)@case('employee')<td class="px-4 py-3 truncate text-sm font-medium">{{ $row->employee?->full_name }}</td>@break @case('course')<td class="px-4 py-3"><p class="truncate text-sm">{{ $row->session?->course?->title }}</p>@if($row->session?->course?->is_mandatory)<span class="text-xs text-amber-700">Zorunlu</span>@endif</td>@break @case('session')<td class="px-4 py-3 text-sm text-slate-600">{{ $row->session?->starts_at?->format('d.m.Y H:i') }}</td>@break @case('progress')<td class="px-4 py-3 text-sm">%{{ $row->progress_percent }}</td>@break @case('score')<td class="px-4 py-3 text-sm">{{ $row->exam_score??'—' }}</td>@break @case('status')<td class="px-4 py-3"><span class="px-2 py-0.5 text-xs font-mono rounded bg-slate-100">{{ match($row->status){'enrolled'=>'Kayıtlı','in_progress'=>'Devam','completed'=>'Tamamlandı','failed'=>'Başarısız',default=>$row->status} }}</span></td>@break @case('certificate')<td class="px-4 py-3 text-xs">{{ $row->certificate?->certificate_number??'—' }}</td>@break @case('actions')<td class="px-4 py-3">@if(!$selfService && auth()->user()?->hasHrPermission('hr.training.manage') && !in_array($row->status,['completed','failed','cancelled'],true))<div class="flex gap-1"><input wire:model.defer="progressValues.{{ $row->id }}" type="number" min="0" max="100" class="w-16 rounded-[6px] border px-2 py-2 text-sm" placeholder="%"><input wire:model.defer="scoreValues.{{ $row->id }}" type="number" min="0" max="100" class="w-16 rounded-[6px] border px-2 py-2 text-sm" placeholder="Puan"><button wire:click="recordResult({{ $row->id }})" class="px-2 py-2 text-xs text-indigo-700">Kaydet</button></div>@endif</td>@break @endswitch @endforeach</tr>@empty<tr><td colspan="{{ count($visibleColumns) }}" class="px-4 py-12 text-center text-sm text-slate-500">Eğitim kaydı yok.</td></tr>@endforelse</tbody></table></div>
<div class="md:hidden divide-y">@forelse($enrollments as $row)<article class="p-4"><div class="flex justify-between gap-2"><div><p class="text-sm font-medium">{{ $row->session?->course?->title }}</p><p class="text-xs text-slate-500">{{ $row->employee?->full_name }} · {{ $row->session?->starts_at?->format('d.m.Y') }}</p></div><span class="px-2 py-0.5 text-xs font-mono rounded bg-slate-100">%{{ $row->progress_percent }}</span></div><p class="mt-3 text-xs text-slate-600">Sınav: {{ $row->exam_score??'—' }} · Sertifika: {{ $row->certificate?->certificate_number??'—' }}</p></article>@empty<div class="p-8 text-center text-sm text-slate-500">Eğitim kaydı yok.</div>@endforelse</div><div class="border-t p-4">{{ $enrollments->links() }}</div></section>
<section class="rounded-[10px] border border-slate-200 bg-white shadow-sm overflow-hidden"><div class="border-b p-4"><h2 class="text-sm font-semibold">Sertifika sicili</h2></div><div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-3 p-4">@forelse($certificates as $certificate)<article class="min-w-0 rounded-[8px] border border-slate-200 bg-slate-50/70 p-4"><div class="flex justify-between gap-2"><p class="truncate text-sm font-medium">{{ $certificate->course?->title }}</p><span class="px-2 py-0.5 text-xs font-mono rounded {{ $certificate->effective_status==='expired'?'bg-red-50 text-red-700':'bg-emerald-50 text-emerald-700' }}">{{ $certificate->effective_status==='expired'?'Süresi doldu':'Geçerli' }}</span></div><p class="mt-1 truncate text-xs text-slate-500">{{ $certificate->employee?->full_name }}</p><p class="mt-3 text-xs font-mono text-slate-700">{{ $certificate->certificate_number }}</p><p class="mt-1 text-xs text-slate-500">{{ $certificate->issued_on->format('d.m.Y') }}{{ $certificate->expires_on?' → '.$certificate->expires_on->format('d.m.Y'):' · süresiz' }}</p></article>@empty<p class="text-sm text-slate-500">Sertifika yok.</p>@endforelse</div></section>
</div>
