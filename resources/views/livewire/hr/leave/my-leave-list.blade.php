<div class="space-y-4 lg:space-y-6"><div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-3"><div><h1 class="text-xl lg:text-2xl font-semibold text-slate-900">İzinlerim</h1><p class="mt-1 text-sm text-slate-500">Bakiye ve izin taleplerinizi buradan takip edebilirsiniz.</p></div><a href="{{ route('hr.my-leaves.create') }}" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] bg-slate-900 text-white text-sm font-medium text-center">+ İzin Talep Et</a></div><section class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-3 lg:gap-4">@forelse($balances as $balance)<article class="rounded-[8px] border border-slate-200 bg-slate-50/70 p-4"><p class="text-sm text-slate-500">{{ $balance->leaveType?->name }}</p><p class="mt-1 text-2xl font-semibold {{ $balance->remaining_amount < 0 ? 'text-red-700' : 'text-slate-900' }}">{{ $balance->remaining_amount }} <span class="text-sm font-normal text-slate-500">{{ $balance->leaveType?->unit?->label() }}</span></p><p class="mt-1 text-xs text-slate-500">{{ $balance->period_year }} dönemi</p></article>@empty<article class="rounded-[8px] border border-slate-200 bg-slate-50/70 p-4 text-sm text-slate-500">Henüz bakiye hareketiniz bulunmuyor.</article>@endforelse</section><section class="rounded-[10px] border border-slate-200 bg-white shadow-sm overflow-hidden"><div class="p-4 border-b border-slate-200"><h2 class="text-sm font-semibold text-slate-900">Taleplerim</h2></div><div class="divide-y divide-slate-100">@forelse($requests as $request)<article class="p-4 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-2"><div><p class="text-sm font-medium text-slate-900">{{ $request->leaveType?->name }}</p><p class="mt-1 text-xs text-slate-500">{{ $request->start_date->format('d.m.Y') }} — {{ $request->end_date->format('d.m.Y') }} · {{ $request->requested_amount }} {{ $request->unit->label() }}</p></div><span class="self-start sm:self-auto px-2 py-0.5 text-xs font-mono rounded bg-slate-100 text-slate-700">{{ $request->status->label() }}</span></article>@empty<div class="p-8 text-center text-sm text-slate-500">Henüz izin talebiniz bulunmuyor.</div>@endforelse</div><div class="p-4 border-t border-slate-200">{{ $requests->links() }}</div></section></div>
