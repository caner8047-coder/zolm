<div class="space-y-4 lg:space-y-6">
    <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-3">
        <div><h1 class="text-xl lg:text-2xl font-semibold text-slate-900">{{ $selfService ? 'Masraflarım' : 'Masraf Yönetimi' }}</h1><p class="mt-1 text-sm text-slate-500">Fişten ödemeye kadar tüm onay zincirini tek defterde izleyin.</p></div>
        @if(!$selfService && auth()->user()?->hasHrPermission('hr.expenses.approve'))<a href="{{ route('hr.settings.expense-categories') }}" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] bg-slate-900 text-center text-sm text-white">Kategori ayarları</a>@endif
    </div>
    @if(session('success'))<div class="rounded-[8px] border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif
    <section class="rounded-[10px] border border-slate-200 bg-white shadow-sm p-4"><div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-3 lg:gap-4"><div class="rounded-[8px] border border-slate-200 bg-slate-50/70 p-4"><p class="text-xs text-slate-500">Kayıt</p><p class="mt-1 text-xl font-semibold text-slate-900">{{ number_format($summary->count ?? 0, 0, ',', '.') }}</p></div><div class="rounded-[8px] border border-slate-200 bg-slate-50/70 p-4"><p class="text-xs text-slate-500">Onay bekleyen</p><p class="mt-1 text-xl font-semibold text-amber-700">{{ number_format($summary->pending_count ?? 0, 0, ',', '.') }}</p></div><div class="rounded-[8px] border border-slate-200 bg-slate-50/70 p-4"><p class="text-xs text-slate-500">Filtre toplamı</p><p class="mt-1 text-xl font-semibold text-slate-900">{{ number_format($summary->total ?? 0, 2, ',', '.') }}</p></div></div></section>
    @if(auth()->user()?->hasHrPermission('hr.expenses.create'))
    <section x-data="{open:{{ $selfService ? 'true' : 'false' }}}" class="rounded-[10px] border border-slate-200 bg-white shadow-sm overflow-hidden">
        <button type="button" @click="open=!open" class="flex w-full items-center justify-between p-4 text-left"><span><span class="block text-sm font-semibold text-slate-900">Yeni masraf talebi</span><span class="mt-1 block text-xs text-slate-500">Net tutarı girin; KDV ve brüt tutar sistem tarafından hesaplanır.</span></span><span x-text="open?'−':'+'"></span></button>
        <form x-show="open" x-cloak wire:submit="create" class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-3 lg:gap-4 border-t border-slate-200 bg-slate-50/60 p-4">
            @if(!$selfService)<select wire:model.defer="employeeId" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2"><option value="">Çalışan seçin</option>@foreach($employees as $employee)<option value="{{ $employee->id }}">{{ $employee->full_name }}</option>@endforeach</select>@endif
            <select wire:model.live="categoryId" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2"><option value="">Kategori seçin</option>@foreach($categories as $category)<option value="{{ $category->id }}">{{ $category->name }}{{ $category->requires_receipt ? ' · belge zorunlu' : '' }}</option>@endforeach</select>
            <input type="date" wire:model.defer="expenseDate" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2">
            <div class="grid grid-cols-3 gap-2"><input type="number" step="0.01" wire:model.defer="netAmount" class="col-span-2 min-w-0 text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2" placeholder="Net tutar"><select wire:model.defer="currency" class="min-w-0 text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-2 py-3 sm:py-2"><option>TRY</option><option>EUR</option><option>USD</option><option>GBP</option></select></div>
            <input type="number" step="0.01" wire:model.defer="vatRate" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2" placeholder="KDV oranı %">
            <input wire:model.defer="merchantName" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2" placeholder="Satıcı / işletme">
            <input wire:model.defer="documentNumber" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2" placeholder="Fiş/fatura no">
            <input wire:model.defer="projectReference" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2" placeholder="Proje referansı">
            <input wire:model.defer="orderReference" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2" placeholder="Sipariş referansı">
            <input wire:model.defer="customerReference" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2" placeholder="Müşteri referansı">
            <textarea wire:model.defer="description" rows="2" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2" placeholder="Masraf açıklaması"></textarea>
            <label class="flex min-h-11 items-center rounded-[6px] border border-slate-200 bg-white px-3 text-sm text-slate-600"><input type="file" wire:model="receipt" accept=".pdf,.jpg,.jpeg,.png,.webp" class="min-w-0 w-full text-sm"></label>
            <button class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] bg-slate-900 text-sm text-white" wire:loading.attr="disabled" wire:target="create,receipt"><span wire:loading.remove wire:target="create">Talep oluştur</span><span wire:loading wire:target="create,receipt">İşleniyor…</span></button>
            @if($errors->any())<p class="sm:col-span-2 xl:col-span-3 text-xs text-red-600">{{ $errors->first() }}</p>@endif
        </form>
    </section>
    @endif
    <section class="rounded-[10px] border border-slate-200 bg-white shadow-sm overflow-hidden">
        <div class="border-b border-slate-200 bg-slate-50/60 p-4"><div class="flex flex-col lg:flex-row lg:items-start lg:justify-between gap-3"><div class="grid min-w-0 flex-1 grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-3">@if(!$selfService)<input wire:model.live.debounce.300ms="search" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2" placeholder="Çalışan, satıcı veya belge ara">@endif<select wire:model.live="statusFilter" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2"><option value="">Tüm durumlar</option><option value="pending_manager">Yönetici bekliyor</option><option value="pending_hr">İK bekliyor</option><option value="approved">Onaylandı</option><option value="paid">Ödendi</option><option value="rejected">Reddedildi</option><option value="cancelled">İptal edildi</option></select><select wire:model.live="categoryFilter" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2"><option value="">Tüm kategoriler</option>@foreach($categories as $category)<option value="{{ $category->id }}">{{ $category->name }}</option>@endforeach</select></div><div x-data="{columns:false}" class="relative"><button @click="columns=!columns" type="button" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] border border-slate-200 bg-white text-sm text-slate-700">Kolonlar · {{ count($visibleColumns) }}</button><div x-show="columns" @click.outside="columns=false" x-cloak class="absolute right-0 z-20 mt-2 w-52 rounded-[8px] border border-slate-200 bg-white p-2 shadow-md">@foreach($columnLabels as $key=>$label)<label class="flex items-center gap-2 px-2 py-2 text-sm text-slate-700"><input type="checkbox" wire:click="toggleColumn('{{ $key }}')" @checked(in_array($key,$visibleColumns,true)) @disabled(in_array($key,['employee','actions'],true)) class="rounded border-slate-300">{{ $label }}</label>@endforeach</div></div></div>@if($search || $statusFilter || $categoryFilter)<p class="mt-3 text-xs text-slate-500">Aktif filtre: {{ $search ?: 'arama yok' }} · {{ $statusFilter ?: 'tüm durumlar' }} · {{ $categoryFilter ? 'kategori seçili' : 'tüm kategoriler' }}</p>@endif</div>
        <div class="hidden md:block overflow-x-auto rounded-lg" x-data="columnResize()"><table class="w-full table-fixed"><thead class="bg-slate-50/60 text-left text-xs uppercase text-slate-500"><tr>@foreach($visibleColumns as $column)<th class="relative px-4 py-3" style="width:{{ in_array($column,['employee','merchant','references'],true)?'190px':(in_array($column,['actions'],true)?'170px':'130px') }}">@if($column==='date')<button wire:click="sortTable('expense_date')">{{ $columnLabels[$column] }}</button>@elseif($column==='amount')<button wire:click="sortTable('gross_amount')">{{ $columnLabels[$column] }}</button>@elseif($column==='status')<button wire:click="sortTable('status')">{{ $columnLabels[$column] }}</button>@else{{ $columnLabels[$column] }}@endif</th>@endforeach</tr></thead><tbody class="divide-y divide-slate-100">@forelse($expenses as $expense)<tr class="hover:bg-slate-50/60">@foreach($visibleColumns as $column)@switch($column)@case('employee')<td class="overflow-hidden text-ellipsis px-4 py-3 text-sm font-medium text-slate-900">{{ $expense->employee?->full_name }}</td>@break @case('date')<td class="px-4 py-3 text-sm text-slate-600">{{ $expense->expense_date->format('d.m.Y') }}</td>@break @case('category')<td class="overflow-hidden text-ellipsis px-4 py-3 text-sm text-slate-600">{{ $expense->category?->name }}</td>@break @case('merchant')<td class="overflow-hidden px-4 py-3"><p class="truncate text-sm text-slate-700">{{ $expense->merchant_name ?: '—' }}</p><p class="truncate text-xs text-slate-500">{{ $expense->document_number ?: 'Belge no yok' }}</p>@if($expense->receipt)<a href="{{ route('hr.expenses.receipt', $expense->receipt) }}" class="text-xs text-indigo-600">Fişi indir</a>@endif</td>@break @case('references')<td class="overflow-hidden px-4 py-3 text-xs text-slate-500"><p class="truncate">{{ $expense->project_reference ?: $expense->order_reference ?: $expense->customer_reference ?: '—' }}</p></td>@break @case('amount')<td class="px-4 py-3"><p class="text-sm font-medium text-slate-900">{{ number_format($expense->gross_amount,2,',','.') }} {{ $expense->currency }}</p><p class="text-xs text-slate-500">KDV {{ number_format($expense->vat_amount,2,',','.') }}</p></td>@break @case('status')<td class="px-4 py-3"><span class="px-2 py-0.5 text-xs font-mono rounded bg-slate-100 text-slate-700">{{ $expense->status->label() }}</span></td>@break @case('actions')<td class="px-4 py-3 text-right">@if(!$selfService && in_array($expense->status->value,['pending_manager','pending_hr'],true) && auth()->user()?->hasHrPermission('hr.expenses.approve'))<button wire:click="startDecision({{ $expense->id }})" class="px-2 py-2 text-xs text-slate-700">Karar</button>@elseif(!$selfService && $expense->status->value==='approved' && auth()->user()?->hasHrPermission('hr.expenses.approve'))<button wire:click="startPayment({{ $expense->id }})" class="px-2 py-2 text-xs text-emerald-700">Öde</button>@endif @if(in_array($expense->status->value,['pending_manager','pending_hr'],true) && ($selfService || auth()->user()?->hasHrPermission('hr.expenses.approve')))<button wire:click="cancel({{ $expense->id }})" wire:confirm="Masraf talebi iptal edilsin mi?" class="px-2 py-2 text-xs text-red-600">İptal</button>@endif</td>@break @endswitch @endforeach</tr>@empty<tr><td colspan="{{ count($visibleColumns) }}" class="px-4 py-12 text-center text-sm text-slate-500">Masraf kaydı bulunamadı.</td></tr>@endforelse</tbody></table></div>
        <div class="md:hidden divide-y divide-slate-100">@forelse($expenses as $expense)<article class="p-4"><div class="flex items-start justify-between gap-3"><div class="min-w-0"><p class="truncate text-sm font-medium text-slate-900">{{ $expense->employee?->full_name }} · {{ $expense->category?->name }}</p><p class="mt-1 text-xs text-slate-500">{{ $expense->expense_date->format('d.m.Y') }} · {{ $expense->merchant_name ?: 'Satıcı yok' }}</p></div><span class="shrink-0 px-2 py-0.5 text-xs font-mono rounded bg-slate-100">{{ $expense->status->label() }}</span></div><p class="mt-3 text-lg font-semibold text-slate-900">{{ number_format($expense->gross_amount,2,',','.') }} {{ $expense->currency }}</p><div class="mt-3 grid grid-cols-1 sm:grid-cols-2 gap-2">@if(!$selfService && in_array($expense->status->value,['pending_manager','pending_hr'],true) && auth()->user()?->hasHrPermission('hr.expenses.approve'))<button wire:click="startDecision({{ $expense->id }})" class="w-full px-4 py-3 rounded-[6px] border border-slate-200 text-sm">Karar ver</button>@elseif(!$selfService && $expense->status->value==='approved' && auth()->user()?->hasHrPermission('hr.expenses.approve'))<button wire:click="startPayment({{ $expense->id }})" class="w-full px-4 py-3 rounded-[6px] bg-slate-900 text-sm text-white">Ödeme kaydet</button>@endif @if(in_array($expense->status->value,['pending_manager','pending_hr'],true) && ($selfService || auth()->user()?->hasHrPermission('hr.expenses.approve')))<button wire:click="cancel({{ $expense->id }})" wire:confirm="Masraf talebi iptal edilsin mi?" class="w-full px-4 py-3 rounded-[6px] border border-red-200 text-sm text-red-700">İptal et</button>@endif</div></article>@empty<div class="p-8 text-center text-sm text-slate-500">Masraf kaydı bulunamadı.</div>@endforelse</div>
        <div class="border-t border-slate-200 p-4">{{ $expenses->links() }}</div>
    </section>
    @if($decidingId)<div class="fixed inset-0 z-50 flex items-end sm:items-center justify-center bg-slate-900/40 p-4"><div class="w-full max-w-md rounded-[10px] border border-slate-200 bg-white p-4 lg:p-6 shadow-md"><h2 class="text-lg font-semibold text-slate-900">Masraf kararı</h2><textarea wire:model.defer="decisionNote" rows="3" class="mt-4 w-full text-base sm:text-sm rounded-[6px] border border-slate-200 px-3 py-3 sm:py-2" placeholder="Karar notu (ret için zorunlu)"></textarea><div class="mt-4 grid grid-cols-1 sm:grid-cols-3 gap-2"><button wire:click="$set('decidingId',null)" class="w-full px-4 py-3 sm:py-2 rounded-[6px] border border-slate-200 text-sm">Vazgeç</button><button wire:click="reject" class="w-full px-4 py-3 sm:py-2 rounded-[6px] border border-red-200 text-sm text-red-700">Reddet</button><button wire:click="approve" class="w-full px-4 py-3 sm:py-2 rounded-[6px] bg-slate-900 text-sm text-white">Onayla</button></div></div></div>@endif
    @if($payingId)<div class="fixed inset-0 z-50 flex items-end sm:items-center justify-center bg-slate-900/40 p-4"><div class="w-full max-w-md rounded-[10px] border border-slate-200 bg-white p-4 lg:p-6 shadow-md"><h2 class="text-lg font-semibold text-slate-900">Ödeme kaydı</h2><input wire:model.defer="paymentReference" class="mt-4 w-full text-base sm:text-sm rounded-[6px] border border-slate-200 px-3 py-3 sm:py-2" placeholder="Banka / kasa işlem referansı"><div class="mt-4 grid grid-cols-1 sm:grid-cols-2 gap-2"><button wire:click="$set('payingId',null)" class="w-full px-4 py-3 sm:py-2 rounded-[6px] border border-slate-200 text-sm">Vazgeç</button><button wire:click="markPaid" class="w-full px-4 py-3 sm:py-2 rounded-[6px] bg-slate-900 text-sm text-white">Ödendi işaretle</button></div></div></div>@endif
</div>
