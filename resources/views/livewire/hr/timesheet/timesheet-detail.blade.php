<div class="space-y-4 lg:space-y-6">
    <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-3"><div><a href="{{ route('hr.timesheets') }}" class="text-xs text-slate-500">← Dönemler</a><h1 class="mt-1 text-xl lg:text-2xl font-semibold text-slate-900">{{ $period->name }}</h1><p class="mt-1 text-sm text-slate-500">{{ $period->starts_on->format('d.m.Y') }} — {{ $period->ends_on->format('d.m.Y') }} · {{ $period->status->label() }}</p></div><div class="flex flex-col sm:flex-row gap-2"><a href="{{ route('hr.timesheets.export',$period->id) }}" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] border border-slate-200 bg-white text-center text-sm text-slate-700">Excel</a>@if($period->status->value!=='closed' && auth()->user()?->hasHrPermission('hr.timesheet.confirm'))<button wire:click="calculate" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] border border-slate-200 bg-white text-sm text-slate-700">Yeniden hesapla</button><button wire:click="confirmAll" wire:confirm="Tüm taslak satırlar onaylansın mı?" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] border border-slate-200 bg-white text-sm text-slate-700">Tümünü onayla</button>@endif @if($period->status->value==='calculated' && auth()->user()?->hasHrPermission('hr.timesheet.close'))<button wire:click="close" wire:confirm="Dönem kapandıktan sonra yalnız revizyonla düzeltilebilir. Devam edilsin mi?" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] bg-slate-900 text-sm font-medium text-white">Dönemi kapat</button>@endif</div></div>
    @if(session('success'))<div class="rounded-[8px] border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif
    <div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-5 gap-3 lg:gap-4">@foreach(['scheduled_minutes'=>'Planlanan','worked_minutes'=>'Çalışılan','leave_minutes'=>'İzin','overtime_minutes'=>'Fazla','missing_minutes'=>'Eksik'] as $key=>$label)<div class="rounded-[8px] border border-slate-200 bg-slate-50/70 p-4"><p class="text-xs text-slate-500">{{ $label }}</p><p class="mt-1 text-lg font-semibold text-slate-900">{{ intdiv($totals[$key],60) }}s {{ $totals[$key]%60 }}dk</p></div>@endforeach</div>
    <section class="rounded-[10px] border border-slate-200 bg-white shadow-sm overflow-hidden"><div class="border-b border-slate-200 bg-slate-50/60 p-4 lg:p-6"><div class="mb-3 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3"><div><h2 class="text-sm font-semibold text-slate-900">Günlük ledger</h2><p class="mt-1 text-xs text-slate-500">Aktif filtre: {{ $search ?: 'yok' }} · {{ $statusFilter ?: 'tüm durumlar' }}</p></div><div x-data="{open:false}" class="relative"><button @click="open=!open" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] border border-slate-200 bg-white text-sm">Kolonlar · {{ count($visibleColumns) }}</button><div x-show="open" x-cloak @click.outside="open=false" class="absolute right-0 z-20 mt-2 w-48 rounded-[8px] border border-slate-200 bg-white p-2 shadow-md">@foreach(['employee'=>'Çalışan','date'=>'Tarih','scheduled'=>'Plan','worked'=>'Çalışma','leave'=>'İzin','overtime'=>'Fazla','missing'=>'Eksik','status'=>'Durum','actions'=>'İşlem'] as $key=>$label)<label class="flex items-center gap-2 px-2 py-2 text-sm"><input type="checkbox" wire:click="toggleColumn('{{ $key }}')" @checked(in_array($key,$visibleColumns,true))>{{ $label }}</label>@endforeach</div></div></div><div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-3"><input wire:model.live.debounce.300ms="search" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2 xl:col-span-2" placeholder="Çalışan veya sicil ara"><select wire:model.live="statusFilter" class="text-base sm:text-sm rounded-[6px] border border-slate-200 bg-white px-3 py-3 sm:py-2"><option value="">Tüm durumlar</option><option value="draft">Taslak</option><option value="confirmed">Onaylandı</option><option value="closed">Kapandı</option></select></div></div>
        <div class="hidden md:block overflow-x-auto rounded-lg" x-data="columnResize()"><table class="w-full table-fixed"><thead class="bg-slate-50/60 text-left text-xs uppercase text-slate-500"><tr>@foreach(['employee'=>'Çalışan','date'=>'Tarih','scheduled'=>'Plan','worked'=>'Çalışma','leave'=>'İzin','overtime'=>'Fazla','missing'=>'Eksik','status'=>'Durum','actions'=>'İşlem'] as $key=>$label)@if(in_array($key,$visibleColumns,true))<th class="px-3 py-3 {{ $key==='actions'?'text-right':'' }}">{{ $label }}</th>@endif @endforeach</tr></thead><tbody class="divide-y divide-slate-100">@forelse($rows as $row)<tr>@if(in_array('employee',$visibleColumns,true))<td class="overflow-hidden text-ellipsis px-3 py-3 text-sm font-medium text-slate-900">{{ $row->employee?->full_name }}</td>@endif @if(in_array('date',$visibleColumns,true))<td class="px-3 py-3 text-sm text-slate-600">{{ $row->work_date->format('d.m.Y') }}</td>@endif @foreach(['scheduled'=>'scheduled_minutes','worked'=>'worked_minutes','leave'=>'leave_minutes','overtime'=>'overtime_minutes','missing'=>'missing_minutes'] as $column=>$field)@if(in_array($column,$visibleColumns,true))<td class="px-3 py-3 text-sm text-slate-600">{{ intdiv((int)$row->effective($field),60) }}:{{ str_pad((int)$row->effective($field)%60,2,'0',STR_PAD_LEFT) }}</td>@endif @endforeach @if(in_array('status',$visibleColumns,true))<td class="px-3 py-3"><span class="px-2 py-0.5 text-xs font-mono rounded bg-slate-100 text-slate-700">{{ $row->status->label() }}</span>@if($row->latestCorrection)<span class="ml-1 text-xs text-amber-700">R{{ $row->latestCorrection->revision_number }}</span>@endif</td>@endif @if(in_array('actions',$visibleColumns,true))<td class="px-3 py-3 text-right">@if($row->status->value==='draft' && auth()->user()?->hasHrPermission('hr.timesheet.confirm'))<button wire:click="confirm({{ $row->id }})" class="px-2 py-2 text-xs text-slate-700">Onayla</button>@endif @if(in_array($row->status->value,['confirmed','closed'],true) && auth()->user()?->hasHrPermission('hr.timesheet.correct'))<button wire:click="startCorrection({{ $row->id }})" class="px-2 py-2 text-xs text-amber-700">Revizyon</button>@endif</td>@endif</tr>@empty<tr><td colspan="{{ count($visibleColumns) }}" class="px-4 py-12 text-center text-sm text-slate-500">Puantaj satırı bulunamadı.</td></tr>@endforelse</tbody></table></div>
        <div class="md:hidden divide-y divide-slate-100">@forelse($rows as $row)<article class="p-4"><div class="flex items-start justify-between gap-3"><div class="min-w-0"><p class="truncate text-sm font-medium text-slate-900">{{ $row->employee?->full_name }}</p><p class="mt-1 text-xs text-slate-500">{{ $row->work_date->format('d.m.Y') }} · Çalışma {{ intdiv((int)$row->effective('worked_minutes'),60) }}s {{ (int)$row->effective('worked_minutes')%60 }}dk</p></div><span class="px-2 py-0.5 text-xs font-mono rounded bg-slate-100">{{ $row->status->label() }}</span></div><div class="mt-3 grid grid-cols-3 gap-2 text-xs text-slate-600"><span>Fazla {{ $row->effective('overtime_minutes') }}dk</span><span>Eksik {{ $row->effective('missing_minutes') }}dk</span><span>İzin {{ $row->effective('leave_minutes') }}dk</span></div>@if($row->status->value==='draft')<button wire:click="confirm({{ $row->id }})" class="mt-3 w-full px-4 py-3 rounded-[6px] border border-slate-200 text-sm">Onayla</button>@elseif(auth()->user()?->hasHrPermission('hr.timesheet.correct'))<button wire:click="startCorrection({{ $row->id }})" class="mt-3 w-full px-4 py-3 rounded-[6px] border border-amber-200 text-sm text-amber-700">Revizyon oluştur</button>@endif</article>@empty<div class="p-8 text-center text-sm text-slate-500">Puantaj satırı bulunamadı.</div>@endforelse</div><div class="border-t border-slate-200 p-4">{{ $rows->links() }}</div>
    </section>
    @if($correctingId)<div class="fixed inset-0 z-50 flex items-end sm:items-center justify-center bg-slate-900/40 p-4"><form wire:submit="saveCorrection" class="w-full max-w-lg rounded-[10px] border border-slate-200 bg-white p-4 lg:p-6 shadow-md"><h2 class="text-lg font-semibold text-slate-900">Puantaj revizyonu</h2><p class="mt-1 text-sm text-slate-500">Ana kayıt değişmez; bu değerler yeni revizyon olarak saklanır.</p><div class="mt-4 grid grid-cols-1 sm:grid-cols-2 gap-3">@foreach(['worked_minutes'=>'Çalışılan dk','break_minutes'=>'Mola dk','leave_minutes'=>'İzin dk','overtime_minutes'=>'Fazla dk','missing_minutes'=>'Eksik dk'] as $field=>$label)<label class="text-xs text-slate-500">{{ $label }}<input type="number" min="0" wire:model.defer="correction.{{ $field }}" class="mt-1 w-full text-base sm:text-sm rounded-[6px] border border-slate-200 px-3 py-3 sm:py-2"></label>@endforeach</div><textarea wire:model.defer="correctionReason" rows="3" class="mt-3 w-full text-base sm:text-sm rounded-[6px] border border-slate-200 px-3 py-3 sm:py-2" placeholder="Zorunlu revizyon gerekçesi"></textarea>@if($errors->any())<p class="mt-2 text-xs text-red-600">{{ $errors->first() }}</p>@endif<div class="mt-4 flex flex-col-reverse sm:flex-row sm:justify-end gap-2"><button type="button" wire:click="$set('correctingId',null)" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] border border-slate-200 text-sm">Vazgeç</button><button class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] bg-slate-900 text-sm text-white">Revizyonu kaydet</button></div></form></div>@endif
</div>
