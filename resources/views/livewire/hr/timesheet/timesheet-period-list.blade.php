<div class="space-y-4 lg:space-y-6">
    <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-3"><div><h1 class="text-xl lg:text-2xl font-semibold text-slate-900">Puantaj Dönemleri</h1><p class="mt-1 text-sm text-slate-500">Günlük çalışma ledger’ını hesaplayın, onaylayın ve aylık dönemi kapatın.</p></div><div class="flex flex-col sm:flex-row gap-2"><a href="{{ route('hr.overtime') }}" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] border border-slate-200 bg-white text-center text-sm text-slate-700">Fazla mesai</a><a href="{{ route('hr.attendance') }}" class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] border border-slate-200 bg-white text-center text-sm text-slate-700">PDKS</a></div></div>
    @if(session('success'))<div class="rounded-[8px] border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif
    @if(auth()->user()?->hasHrPermission('hr.timesheet.confirm'))<form wire:submit="create" class="rounded-[10px] border border-slate-200 bg-white p-4 lg:p-6 shadow-sm"><div class="mb-3"><h2 class="text-sm font-semibold text-slate-900">Yeni dönem</h2><p class="mt-1 text-xs text-slate-500">Çakışmayan en fazla 32 günlük bir aralık seçin.</p></div><div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4 gap-3 lg:gap-4"><input wire:model.defer="name" class="text-base sm:text-sm rounded-[6px] border border-slate-200 px-3 py-3 sm:py-2" placeholder="Dönem adı"><input type="date" wire:model.defer="startsOn" class="text-base sm:text-sm rounded-[6px] border border-slate-200 px-3 py-3 sm:py-2"><input type="date" wire:model.defer="endsOn" class="text-base sm:text-sm rounded-[6px] border border-slate-200 px-3 py-3 sm:py-2"><button class="w-full sm:w-auto px-4 py-3 sm:py-2 rounded-[6px] bg-slate-900 text-sm font-medium text-white">Dönem oluştur</button></div>@if($errors->any())<p class="mt-2 text-xs text-red-600">{{ $errors->first() }}</p>@endif</form>@endif
    <section class="rounded-[10px] border border-slate-200 bg-white shadow-sm overflow-hidden"><div class="border-b border-slate-200 bg-slate-50/60 p-4"><h2 class="text-sm font-semibold text-slate-900">Dönem ledger’ı</h2></div><div class="divide-y divide-slate-100">@forelse($periods as $period)<article class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 p-4"><div class="min-w-0"><div class="flex items-center gap-2"><p class="truncate text-sm font-medium text-slate-900">{{ $period->name }}</p><span class="px-2 py-0.5 text-xs font-mono rounded {{ $period->status->value==='closed'?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-700' }}">{{ $period->status->label() }}</span></div><p class="mt-1 text-xs text-slate-500">{{ $period->starts_on->format('d.m.Y') }} — {{ $period->ends_on->format('d.m.Y') }} · {{ $period->timesheets_count }} satır</p></div><div class="flex flex-col sm:flex-row gap-2">@if($period->status->value!=='closed' && auth()->user()?->hasHrPermission('hr.timesheet.confirm'))<button wire:click="calculate({{ $period->id }})" class="w-full sm:w-auto px-3 py-2 rounded-[6px] border border-slate-200 text-xs text-slate-700">Hesapla</button>@endif<a href="{{ route('hr.timesheets.show',$period->id) }}" class="w-full sm:w-auto px-3 py-2 rounded-[6px] bg-slate-900 text-center text-xs text-white">Dönemi aç</a></div></article>@empty<div class="p-8 text-center text-sm text-slate-500">Henüz puantaj dönemi yok.</div>@endforelse</div></section>
</div>
